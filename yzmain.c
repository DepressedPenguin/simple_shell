#include "yzshell.h"
/**
 * main - Entry point of the yzshell program.
 * @argc: The number of command-line arguments.
 * @argv: An array of strings containing.
 * Return: Always returns 0.
 */
int main(int argc __attribute__((unused)), char **argv)
{
zak = argv;
run_mycode();
return (0);
}
